[header]Step 5 — shell integration[/header]

This pack has shell snippets or a [pack-name]bin/[/pack-name] directory. None of it
reaches your shell until your shell loads dodot's init script.

{% if shell_integration.line_present %}[deployed]✓ already wired up — nothing to do.[/deployed]
{% else %}[warning]Not wired up yet.[/warning] One command does it and then starts a shell to
check that it worked:

  [message]dodot install --write[/message]

Run that once, after the tutorial. Or add the line by hand to
[pack-name]{{ shell_integration.rc_path }}[/pack-name] — I can write it for you, copy it to
clipboard, or skip:

  [message]{{ shell_integration.eval_line }}[/message]{% endif %}
