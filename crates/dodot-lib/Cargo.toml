[package]
name = "dodot-lib"
version = "0.1.0"
edition = "2021"
description = "Core library for dodot dotfiles manager"

[dependencies]
tempfile = { version = "3", optional = true }
thiserror = "2"

[dev-dependencies]
tempfile = "3"

[features]
default = []
test-utils = ["tempfile"]
