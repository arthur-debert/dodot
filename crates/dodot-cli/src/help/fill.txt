[header]dodot fill[/header] — Add placeholder files to an existing pack.

[desc]For each default handler that the pack does [item]not[/item] yet have a file for
([item]install.sh[/item], [item]aliases.sh[/item], [item]Brewfile[/item]), drop in a documented
template file. Existing files are never overwritten. No [item]bin/[/item]
directory is created — if you want a path-handler [item]bin/[/item], make it
yourself ([item]mkdir <pack>/bin[/item]).

Useful when you started a pack as just a config file and now want to
attach shell aliases or a Brewfile to it.[/desc]

[header]USAGE[/header]
  [usage]dodot fill <PACK>[/usage]

[header]ARGUMENTS[/header]
  [item]<PACK>[/item]   [desc]An existing pack to fill in[/desc]

[header]EXAMPLES[/header]
  [example]dodot fill git                 [dim]# add missing handler placeholders to the git pack[/dim][/example]

[header]SAFETY LOCK[/header]
  [desc][item]fill[/item] writes inside the selected root, so its first use on an implicitly
  discovered, unapproved root asks for approval before creating placeholders.
  A non-interactive invocation refuses. For automation, set a valid
  [item]DOTFILES_ROOT[/item] explicitly. Read-only [item]list[/item] / [item]status[/item] remain
  available without approval. Inspect or revoke approvals with [item]dodot roots[/item].[/desc]

[header]SEE ALSO[/header]
  [item]dodot init[/item]    [desc]Create a [item]new[/item] pack ([item].dodot.toml[/item] only; [item]fill[/item] adds the rest)[/desc]
  [item]dodot status[/item]  [desc]Confirm the pack picks up the new files[/desc]
