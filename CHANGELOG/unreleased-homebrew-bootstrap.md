- feat: on macOS, the generated `dodot-init.sh` now opens with Homebrew's environment, captured from `brew shellenv` at `dodot up` time and baked in as static text — so brew stays the authority on its own bootstrap and your shell never runs `brew` to find out (#291). The block is emitted verbatim and first, above the PATH additions your packs contribute, which makes a hand-written `001-homebrew` bootstrap pack unnecessary: brew-installed commands are on PATH for every pack script with no ordering work. Per-shell under a guard, so the zsh-only `fpath`/`FPATH` lines never reach `sh` or `bash`. Nothing is emitted when brew is absent (the next `up` heals it) or off macOS. This is the first command execution dodot puts on the shell startup path — brew's own block re-execs `path_helper`, measured at 2-3 ms per shell start — and `[shell] homebrew = "off"` turns it off.
